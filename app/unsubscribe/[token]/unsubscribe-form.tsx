'use client';
import {useState} from 'react';
export default function UnsubscribeForm({token}:{token:string}){const [done,setDone]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');return done?<p>You have been unsubscribed.</p>:<><p>Confirm below to stop receiving Hall Bible Commentary email updates.</p><button className="button primary" disabled={busy} onClick={async()=>{setBusy(true);setError('');try{const r=await fetch('/api/unsubscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});if(!r.ok)throw Error('Unable to unsubscribe. Please try again.');setDone(true)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}>{busy?'Updating…':'Unsubscribe'}</button>{error&&<p role="alert">{error}</p>}</>}
