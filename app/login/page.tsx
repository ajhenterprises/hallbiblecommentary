'use client';
import {useState} from 'react';import {Header,Footer} from '../components';
export default function Login(){const [message,setMessage]=useState(''),[busy,setBusy]=useState(false);return <><Header/><main className="page detail"><span className="eyebrow">THE WRITING DESK</span><h1>Sign in to your library.</h1><p>Use the administrator account configured for this site.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setMessage('');const data=new FormData(e.currentTarget);try{const r=await fetch('/api/auth',{method:'POST',body:JSON.stringify(Object.fromEntries(data)),headers:{'Content-Type':'application/json'}});const d:any=await r.json();if(!r.ok)throw Error(d.error);location.href='/admin'}catch(e){setMessage((e as Error).message)}finally{setBusy(false)}}}><label className="field">Email<input name="email" type="email" autoComplete="username"/></label><label className="field">Password<input name="password" type="password" autoComplete="current-password"/></label><p className="help">In the local demo, leave these fields blank to open your private development workspace.</p><button className="button primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><button className="text-button" style={{marginTop:20}} onClick={async()=>{await fetch('/api/auth',{method:'DELETE'});location.href='/'}}>Sign out</button>{message&&<p role="alert" className="notice">{message}</p>}</main><Footer/></>}
