import type {MetadataRoute} from 'next';import {books} from '../lib/bible';import {published} from '../lib/store';
export const dynamic='force-dynamic';
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const origin=process.env.SITE_URL||'https://hallbiblecommentary.vercel.app';let entries:any[]=[];try{entries=await published()}catch{}return [...['','/resources/books','/about/aaron-joseph-hall','/about/speaking','/about/permissions','/about/faq','/bible','/commentary','/articles','/topics','/people','/series','/sermons','/about'].map(p=>({url:origin+p})),...books.flatMap(b=>[{url:origin+'/bible/'+b.slug},...Array.from({length:b.chapters},(_,i)=>({url:origin+'/bible/'+b.slug+'/'+(i+1)}))]),...entries.map(c=>({url:origin+'/'+(c.kind==='blog'?'articles':c.kind)+'/'+c.slug,lastModified:c.updated_at}))]}
