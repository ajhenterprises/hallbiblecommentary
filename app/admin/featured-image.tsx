'use client';
import {useState} from 'react';
export default function FeaturedImage({value='',onChange}:{value?:string;onChange:(url:string)=>void}){const [busy,setBusy]=useState(false),[error,setError]=useState('');return <section className="upload-box"><h3>Featured image</h3><p>Upload an image for this entry. It appears above the published teaching.</p>{value&&<img src={value} alt="Featured image preview" style={{maxWidth:320,width:'100%',borderRadius:8}}/>}<label className="field">Choose image<input type="file" accept="image/jpeg,image/png,image/webp" disabled={busy} onChange={async e=>{const f=e.target.files?.[0];if(!f)return;setBusy(true);setError('');try{const form=new FormData();form.set('file',f);const r=await fetch('/api/media',{method:'POST',body:form});const data:any=await r.json();if(!r.ok)throw Error(data.error);onChange(data.url)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}/></label>{busy&&<p role="status">Uploading…</p>}{error&&<p role="alert">{error}</p>}{value&&<button type="button" className="button" onClick={()=>onChange('')}>Remove from this entry</button>}<p className="help">Save the entry to apply changes. Removing an image here does not delete its stored file.</p></section>}
